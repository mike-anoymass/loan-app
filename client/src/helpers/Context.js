import { createContext } from "react";

export const AuthContext = createContext("") //to handle authentication context variables
export const LoanContext = createContext("") //to handle loan application variables